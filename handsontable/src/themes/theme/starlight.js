import mainIcons from '../static/variables/icons/main';
import starlightColors from '../static/variables/colors/starlight';
import starlightTokens from '../static/variables/tokens/starlight';

const starlightTheme = {
  name: 'starlight',
  density: 'comfortable',
  icons: mainIcons,
  colors: starlightColors,
  tokens: starlightTokens,
};

export { starlightTheme };
