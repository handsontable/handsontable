
module.exports = function buildCustom(grunt) {
  'use strict';

  grunt.registerTask('build-custom', function() {
    var
      args = this.args;

    grunt.log.writeln('Creating custom build...\n');
  });
};
