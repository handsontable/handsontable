{
  "issuesOrigin": "private",
  "title": "Writing a plain value into an `autocomplete` or `dropdown` cell whose `source` is an array of `key` and `value` objects now stores the matching object, not the bare label. This makes a paste, `setDataAtCell()` and autofill behave like the cell editor, which already resolved the label, and stops a `strict` column marking a valid pasted option as invalid.",
  "type": "changed",
  "issueOrPR": 13458,
  "breaking": true,
  "framework": "none"
}
