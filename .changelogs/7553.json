{
  "issuesOrigin": "public",
  "title": "Fixed a bug where a cell marked as invalid lost its highlight after `updateSettings()` was called with the `cell`, `cells`, or `columns` option, while the cell kept the invalid value. In React, this happened on every re-render of a grid that has `HotColumn` children.",
  "type": "fixed",
  "issueOrPR": 7553,
  "breaking": false,
  "framework": "none"
}
