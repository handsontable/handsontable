{
  "issuesOrigin": "private",
  "title": "Made a vertical scroll keep the elements of the rows that stay rendered, so a cell in `renderMode: 'onChange'` is not repainted by the scroll and a renderer that caches per element keeps its cache. With merged cells, only the cells of the merged blocks repaint.",
  "type": "changed",
  "issueOrPR": 13467,
  "breaking": false,
  "framework": "none"
}
