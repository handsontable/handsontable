{
  "issuesOrigin": "private",
  "title": "Fixed the grid failing to construct under a Content Security Policy that enforces Trusted Types (`require-trusted-types-for 'script'`). Handsontable now builds its own markup as DOM nodes, so it needs no Trusted Types policy and no entry in your `trusted-types` directive, and the `sanitizer` option may return a `TrustedHTML`.",
  "type": "fixed",
  "issueOrPR": 13244,
  "breaking": false,
  "framework": "none"
}
