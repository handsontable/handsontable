{
  "issuesOrigin": "private",
  "title": "Improved the `updateSettings()` performance with the `formulas` plugin enabled: a setting that changes the row count, such as toggling `nestedRows`, now rebuilds the formula sheet once, after the plugins update, instead of twice. A rebuild that fails now surfaces from the first read or render that needed it, and a listener that reads a cell from `afterUpdateSettings` pays that rebuild inside the read.",
  "type": "changed",
  "issueOrPR": 13600,
  "breaking": false,
  "framework": "none"
}
