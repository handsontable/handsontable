{
  "issuesOrigin": "public",
  "title": "Fixed merged cells drawing on top of the rows below them after a collapsed `nestedRows` parent, a filter, or `trimRows` removed rows from inside the merge. A merged cell now spans only the rows of its own that are still shown, and is restored to its full span once they come back.",
  "type": "fixed",
  "issueOrPR": 7686,
  "breaking": false,
  "framework": "none"
}
