A new option/method was added: $ARGUMENTS

1. Find its JSDoc in the source
2. Generate a docs entry following the format in /docs/api/options.md
3. Check if any migration guide needs updating
4. Update the TypeScript definition in /handsontable/types/
